import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#ff9e80",
    },
    secondary: {
      main: "#ffd180",
      dark: "#F2F2F2",
    },
    common: {
      white: "#fff",
    },
  },
});

export default theme;
